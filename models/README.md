# Place your model here

Put your exported anatomical skeleton at:

```
models/skeleton.glb
```

The app loads this file automatically. Until it exists, a clearly-marked
procedural **placeholder** is shown so you can verify the lighting and
rendering. See the main `../README.md` for exactly which model to download
and how to export it as GLB.
